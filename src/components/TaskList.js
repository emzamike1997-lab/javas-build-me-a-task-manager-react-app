import React from 'react';
import Task from './Task';

function TaskList() {
  return (
    <div>
      <h2>Task List</h2>
      <Task />
    </div>
  );
}

export default TaskList;

This is the basic structure for the task manager React app. We will continue to build on this structure in the next stages.